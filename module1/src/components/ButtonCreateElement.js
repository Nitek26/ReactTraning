import React from 'react';
class ButtonCreateElement extends React.Component {
    render() {
        return React.createElement('button', null, `A to jest component  bez jsx`);
    }
}

export default ButtonCreateElement;